# argocd/apps/
# ArgoCD Application manifests for each environment's OpenClaw deployment.
# Phase 5 (TASK-017 through TASK-020) creates the Application YAML files here.
# Apply with: kubectl apply -f argocd/apps/<env>-openclaw.yaml
