# bootstrap/ manifests are applied from workloads/prod/openclaw/bootstrap/*.yaml
# by scripts/seed-openclaw-aks.sh before ArgoCD sync. Do NOT include crds/ in
# the Helm render or ArgoCD sync — it holds unsubstituted placeholder files and
# is not part of the bootstrap flow.
crds/
