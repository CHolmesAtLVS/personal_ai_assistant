# DEPRECATED — NFS storage removed 2026-04-12.
# storage-aks.tf was deleted; pods now use managed-csi-premium dynamic provisioning
# (storageClass: managed-csi-premium, disk.csi.azure.com, 10 Gi, ReadWriteOnce).
# No static PV is required — Kubernetes provisions the PVC automatically on first pod start.
# See ARCHITECTURE.md for current storage configuration.
#
# DO NOT APPLY this manifest. It targets a storage account and NFS shares that no longer exist.
